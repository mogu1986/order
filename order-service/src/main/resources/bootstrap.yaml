spring:
  application:
    name: order
  profiles:
    active: local
  cloud:
    nacos:
      config:
        file-extension: yaml
        server-addr: 192.168.123.176:8848
        ext-config:
          - data-id: common.yaml
            group: ops
            refresh: true
          - data-id: sentinel.yaml
            group: ops
            refresh: true