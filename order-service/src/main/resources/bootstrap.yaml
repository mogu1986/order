spring:
  application:
    name: order
  profiles:
    active: local
  sleuth:
    sampler:
      probability: 1.0
  cloud:
    nacos:
      config:
        file-extension: yaml
        server-addr: 192.168.123.176:8848
        ext-config:
          - data-id: common.yaml
            group: ops
            refresh: true
          - data-id: sentinel.yaml
            group: ops
            refresh: true
  zipkin:
    base-url: http://zipkin.jq.cn