spring:
  application:
    name: order
  profiles:
    active: local
  cloud:
    nacos:
      config:
        server-addr: 192.168.10.200:8848
        file-extension: yaml
        extension-configs:
          - data-id: redis
            group: ops
            refresh: true
      discovery:
        server-addr: 192.168.10.200:8848